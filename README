gmail_add_emails.py - Adds a file of emails into a gmail contact list.

Usage:
  ./gmail_add_emails.py youraccount@gmail.com file_of_emails.txt

Dependencies:
 * mechanize
   $ easy_install mechanize

 * libgmail
   $ wget http://downloads.sourceforge.net/libgmail/libgmail-0.1.11.tar.gz
   $ tar zxvf libgmail-0.1.11.tar.gz
   $ sudo python libgmail-0.1.11/setup.py install 

Known Bugs:
 * After about 2,000 records gmail will probably stop processing records, at which point you can stop the script and run it again (it will resume where it left off).
